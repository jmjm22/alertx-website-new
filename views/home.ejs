<!DOCTYPE html>
<html lang="he" dir="rtl">
<head>
  <meta charset="UTF-8" />
  <title><%= typeof title !== 'undefined' ? title : 'AlertX' %></title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <style>
    :root {
      --blue: #2196F3;
      --green: #4CAF50;
      --red: #F44336;
      --bg: #f7f7f7;
      --card: #fff;
      --muted: #999;
      --shadow: 0 2px 6px rgba(0,0,0,0.1);
    }
    * { box-sizing: border-box; }
    body { font-family: 'Segoe UI', Arial, sans-serif; background: var(--bg); margin: 0; direction: rtl; color: #222; }

    header { 
      background: var(--card); 
      box-shadow: var(--shadow); 
      padding: 16px 20px; 
      margin-bottom: 20px; 
      text-align: center;
    }
    header h1 { 
      margin: 0; 
      color: var(--blue); 
      font-size: 2rem; 
      letter-spacing: 0.5px;
    }

    main { padding: 0 20px 24px; }

    /* ===== Welcome Card ===== */
    .hero-card {
      background: rgba(255,255,255,0.9);
      backdrop-filter: blur(16px);
      border: 1px solid #e5e7eb;
      box-shadow: 0 10px 30px rgba(0,0,0,0.08);
      border-radius: 20px;
      text-align: center;
      padding: 50px 30px;
      max-width: 700px;
      margin: 60px auto;
      position: relative;
      overflow: hidden;
    }
    .hero-card::before,
    .hero-card::after{
      content:"";
      position:absolute;
      width:460px; height:460px; border-radius:50%;
      filter: blur(10px);
      opacity:.9;
    }
    .hero-card::before{
      right:-18%; bottom:-40%;
      background: radial-gradient(ellipse at center, rgba(33,150,243,.10), transparent 60%);
    }
    .hero-card::after{
      left:-18%; top:-42%;
      background: radial-gradient(ellipse at center, rgba(79,70,229,.08), transparent 60%);
    }

    .hero-title {
      font-size: 2.2rem;
      margin: 0 0 14px;
      color: #0f172a;
      position: relative; z-index:1;
    }
    .hero-title span { color: var(--blue); }

    .hero-sub {
      font-size: 1.1rem;
      color: #475569;
      margin: 0 auto 28px;
      line-height: 1.7;
      max-width: 62ch;
      position: relative; z-index:1;
    }

    .hero-btn {
      display: inline-block;
      background: linear-gradient(135deg, var(--blue), #4f46e5);
      color: #fff;
      font-weight: 700;
      text-decoration: none;
      padding: 14px 34px;
      border-radius: 999px;
      font-size: 1.1rem;
      box-shadow: 0 8px 20px rgba(33,150,243,.3);
      transition: transform .2s, box-shadow .2s;
      position: relative; z-index:1;
    }
    .hero-btn:hover {
      transform: translateY(-3px);
      box-shadow: 0 12px 26px rgba(33,150,243,.4);
    }

    /* Responsive */
    @media (max-width:600px){
      .hero-card{ padding: 36px 22px; margin: 28px 10px; }
      .hero-title{ font-size: 1.8rem; }
      .hero-sub{ font-size: 1rem; }
      .hero-btn{ padding: 12px 24px; font-size: 1rem; }
    }
  </style>
</head>
<body>

  <main>
    <section class="hero-card">
      <h2 class="hero-title">ברוכים הבאים ל־<span>AlertX</span></h2>
      <p class="hero-sub">
        המערכת החכמה לניהול מצבי חירום, שליחת התראות בזמן אמת ומעקב אחר התקדמות הטיפול – הכל בלחיצת כפתור אחת.
      </p>
      <a href="/alerts" class="hero-btn">לעמוד ההתראות</a>
    </section>
  </main>
</body>
</html>
